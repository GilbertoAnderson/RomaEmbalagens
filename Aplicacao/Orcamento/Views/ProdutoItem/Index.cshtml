@model IEnumerable<ProdutoItemModel>
@{
    ViewData["Title"] = "ProdutoItem";
}

<div class="py-5">
    <div class="container">

        <div class="row g-3">
            <div class="col-11">
                <div class="text-left">
                    <h1 class="display-4">Lista de Itens do Produto</h1>
                </div>
            </div>
            <div class="col-1">
                <div class="text-left">
                    <a href="  @Url.Action("Index", "Admin")   " title='Home' style="text-decoration:none">
                        <h3><i class="bi bi-x-circle-fill" title="Retornar"></i></h3>
                    </a>
                </div>
            </div>
        </div>
        <div class="text-left ">
            <form method="post" asp-action="Index" asp-controller="ProdutoItem" enctype="multipart/form-data">
                @Html.TextBox("filtro") &nbsp;
                <input type="submit" value="Procurar" class="btn btn-secondary" /> &nbsp;
                <a href="@Url.Action("Create")"> <input type="button" value="Novo" class="btn btn-primary" /></a>
            </form>
        </div>
        <hr />
        <div class="row">


            @if (Model != null)
            {
                @if (Model.Count() > 0)
                {
                    @foreach (var produto in Model)
                    {

                        @* 
                        <div class="card w-80">
                            <div class="card-body">
                                <h3 class="card-title"> @produto.itensProduto.Nome </h3>
                                <p class="card-text">
                                    <div style="border:2px">
                                        <span>@produto.Quantidade </span>
                                        <span>@produto.Valor </span>
                                        <span>@produto.itensProduto.idUnidade </span>
                                    </div>

                                </p>
                                <a href="@Url.Action("Edit", new { id = produto.idProdutoItem }) " class="btn btn-secondary">editar ...</a>
                            </div>
                        </div> 
                        *@

                        <div class="card w-80">
                            <div class="card-body">
                                <h3 class="card-title"> @produto.idProduto </h3>
                                <p class="card-text">
                                    <div style="border:2px">
                                        <span>@produto.Quantidade </span>
                                        <span>@produto.Valor </span>
                                    </div>

                                </p>
                                <a href="@Url.Action("Edit", new { id = produto.idProdutoItem }) " class="btn btn-secondary">editar ...</a>
                            </div>
                        </div>

                    }
                }
            }
        </div>
    </div>
</div>











